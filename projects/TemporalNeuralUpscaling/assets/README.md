# Assets

Do not add media without recording its source and license.

P1.2 uses a test image generated entirely by `pipeline.image_pipeline.create_synthetic_rgb_image`. The generated geometric/color pattern is original project output, requires no external asset, and may be regenerated deterministically with the documented CLI command.
